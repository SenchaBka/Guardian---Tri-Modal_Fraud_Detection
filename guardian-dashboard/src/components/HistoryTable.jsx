const sc=s=>s>=0.71?"text-danger":s>=0.31?"text-amber":"text-teal";const dt={approve:"tag-teal",review:"tag-amber",block:"tag-danger"};const mp={full:"mode-pill-full",partial:"mode-pill-partial",fallback:"mode-pill-fallback"};export default function HistoryTable({history}){if(!history.length)return<div className="py-8 text-center text-[10px] font-mono text-ink-dim">No analyses yet — run your first transaction above</div>;return<div className="overflow-x-auto"><table className="w-full border-collapse"><thead><tr className="border-b border-border">{["Transaction","Amount","Score","Mode","Decision"].map(h=><th key={h} className="text-left text-[9px] font-mono uppercase tracking-widest text-ink-dim px-3 py-2 font-normal">{h}</th>)}</tr></thead><tbody>{history.map((t,i)=><tr key={i} className="border-b border-border last:border-0 hover:bg-bg-3 transition-colors"><td className="px-3 py-2 font-mono text-[10px] text-ink-muted">{t.id}</td><td className="px-3 py-2 font-mono text-[10px]">${(+t.amount).toLocaleString()}</td><td className={`px-3 py-2 font-mono text-[10px] font-medium ${sc(t.score)}`}>{(t.score*100).toFixed(1)}%</td><td className="px-3 py-2"><span className={`mode-pill ${mp[t.mode]}`}>{t.mode}</span></td><td className="px-3 py-2"><span className={`tag ${dt[t.decision]}`}>{t.decision}</span></td></tr>)}</tbody></table></div>;}
